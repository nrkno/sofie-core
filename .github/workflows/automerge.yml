name: Merge master to staging

on:
  schedule:
    - cron: '30 23 * * *' # every night
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.6
        with:
          owner: tv2
          head: master
          base: staging
          pr_title: Automerge master
          auto_merge: false